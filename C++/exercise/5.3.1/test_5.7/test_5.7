if(ival1 != ival2)
	ival1 = ival2 ; //加一个冒号
else 
	ival1 = ival2 = 0 ;

if (ival < minval) {
	minval = ival ;
	occurs = 1 ;
}  //加花括号

if(int ival = get_value()) {
	cout << "ival = " << ival << endl ;
	if (!val)
		cout << "ival = 0\n" ;
}

if (ival == 0)
	ival = get_value() ;


	
